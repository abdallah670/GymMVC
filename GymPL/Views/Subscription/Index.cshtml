@using GymBLL.ModelVM.Financial
@model IEnumerable<SubscriptionVM>
@{
    ViewData["Title"] = "Manage Subscriptions";
}

<div class="container-fluid p-4" style="max-width: 1400px;">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-1" style="color: var(--ink); font-weight: 700;">Subscriptions</h1>
            <p style="color: var(--muted); opacity: 0.9; margin-bottom: 0;">Manage member subscriptions and status</p>
        </div>
        
        <!-- Filter Dropdown -->
        <div class="dropdown">
            <button class="btn btn-light dropdown-toggle" type="button" id="filterDropdown" data-bs-toggle="dropdown">
                <i class="fas fa-filter me-2"></i> Filter: @(ViewBag.CurrentFilter == "All" ? "All Statuses" : ViewBag.CurrentFilter)
            </button>
            <ul class="dropdown-menu dropdown-menu-end">
                <li><a class="dropdown-item @(ViewBag.CurrentFilter == "All" ? "active" : "")" asp-action="Index" asp-route-status="All">All Statuses</a></li>
                <li><a class="dropdown-item @(ViewBag.CurrentFilter == "Active" ? "active" : "")" asp-action="Index" asp-route-status="Active">Active</a></li>
                <li><a class="dropdown-item @(ViewBag.CurrentFilter == "Expired" ? "active" : "")" asp-action="Index" asp-route-status="Expired">Expired</a></li>
                <li><a class="dropdown-item @(ViewBag.CurrentFilter == "Cancelled" ? "active" : "")" asp-action="Index" asp-route-status="Cancelled">Cancelled</a></li>
            </ul>
        </div>
    </div>

     <!-- Alert Messages -->
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert" 
             style="border-radius: var(--radius); border: none; background-color: #d1e7dd; color: #0f5132;">
            <i class="fas fa-check-circle me-2"></i> @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert"
             style="border-radius: var(--radius); border: none; background-color: #f8d7da; color: #842029;">
            <i class="fas fa-exclamation-circle me-2"></i> @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="card border-0 shadow-sm" style="border-radius: var(--radius); overflow: hidden; background: var(--tile); border: 1px solid var(--frame) !important;">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0" style="color: var(--ink); border-color: var(--rule); background: transparent;">
                <thead style="background: var(--frame);">
                    <tr>
                        <th class="px-4 py-3 border-0 small fw-bold text-uppercase" style="color: var(--ink); opacity: 0.8;">Member</th>
                        <th class="px-4 py-3 border-0 small fw-bold text-uppercase" style="color: var(--ink); opacity: 0.8;">Plan</th>
                        <th class="px-4 py-3 border-0 small fw-bold text-uppercase text-center" style="color: var(--ink); opacity: 0.8;">Status</th>
                        <th class="px-4 py-3 border-0 small fw-bold text-uppercase text-center" style="color: var(--ink); opacity: 0.8;">Starts</th>
                        <th class="px-4 py-3 border-0 small fw-bold text-uppercase text-center" style="color: var(--ink); opacity: 0.8;">Ends</th>
                        <th class="px-4 py-3 border-0 text-end small fw-bold text-uppercase" style="color: var(--ink); opacity: 0.8;">Actions</th>
                    </tr>
                </thead>
                <tbody style="background: transparent;">
                    @foreach (var item in Model)
                    {
                        <tr style="border-bottom: 1px solid var(--rule); background: transparent;">
                            <td class="px-4 py-3">
                                <div class="d-flex align-items-center">
                                    <div class="rounded-circle me-3 d-flex align-items-center justify-content-center" style="width: 35px; height: 35px; background: var(--frame); color: var(--main-color); font-weight: 700;">
                                        @item.MemberName.Substring(0, 1).ToUpper()
                                    </div>
                                    <span class="fw-bold" style="color: var(--ink);">@item.MemberName</span>
                                </div>
                            </td>
                            <td class="px-4 py-3">
                                <span class="badge bg-primary bg-opacity-10 text-primary">@item.MembershipType</span>
                            </td>
                            <td class="px-4 py-3 text-center">
                                @if (item.Status == "Active")
                                {
                                    <span class="badge bg-success bg-opacity-10 text-success rounded-pill px-3" style="font-weight: 600;">Active</span>
                                }
                                else if (item.Status == "Expired")
                                {
                                    <span class="badge bg-secondary bg-opacity-10 text-secondary rounded-pill px-3" style="font-weight: 600;">Expired</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger bg-opacity-10 text-danger rounded-pill px-3" style="font-weight: 600;">@item.Status</span>
                                }
                            </td>
                            <td class="px-4 py-3 text-center" style="color: var(--ink); opacity: 0.95; font-weight: 600;">
                                @item.StartDate.ToString("MMM dd, yyyy")
                            </td>
                            <td class="px-4 py-3 text-center" style="color: var(--ink); opacity: 0.95; font-weight: 600;">
                                @item.EndDate.ToString("MMM dd, yyyy")
                            </td>
                            <td class="px-4 py-3 text-end">
                                <div class="dropdown">
                                    <button class="btn btn-sm rounded-circle d-flex align-items-center justify-content-center" 
                                            style="width: 32px; height: 32px; background: var(--frame); color: var(--ink); border: 1px solid var(--rule);" 
                                            type="button" data-bs-toggle="dropdown">
                                        <i class="fas fa-ellipsis-v"></i>
                                    </button>
                                    <ul class="dropdown-menu dropdown-menu-end border-0 shadow-sm">
                                        <li>
                                            <a class="dropdown-item" asp-action="Details" asp-route-id="@item.Id">
                                                <i class="fas fa-eye me-2 text-primary"></i> View Details
                                            </a>
                                        </li>
                                        @if (item.Status == "Active")
                                        {
                                            <li>
                                                <form asp-action="Cancel" asp-route-id="@item.Id" method="post" style="display:inline;">
                                                    <button type="submit" class="dropdown-item text-warning" onclick="return confirm('Are you sure you want to cancel this subscription?');">
                                                        <i class="fas fa-ban me-2"></i> Cancel
                                                    </button>
                                                </form>
                                            </li>
                                        }
                                        <li><hr class="dropdown-divider"></li>
                                        <li>
                                            <form asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline;">
                                                <button type="submit" class="dropdown-item text-danger" onclick="return confirm('Are you sure you want to delete this subscription log? This is irreversible.');">
                                                    <i class="fas fa-trash-alt me-2"></i> Delete
                                                </button>
                                            </form>
                                        </li>
                                    </ul>
                                </div>
                            </td>
                        </tr>
                    }
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="6" class="text-center py-5 text-muted">
                                <i class="fas fa-inbox fa-3x mb-3 opacity-25"></i>
                                <p class="mb-0">No subscriptions found with status: <strong>@ViewBag.CurrentFilter</strong></p>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
